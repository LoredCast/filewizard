# Whitelist: only what the Dockerfile copies is sent to the builder, so local files
# (config, uploads, databases, venvs, ...) can neither leak into nor change the image.
*
!main.py
!settings.default.yml
!templates/
!static/
!locks/
!docker/
**/*.old
