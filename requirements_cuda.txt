# CUDA image: everything from the full image plus the CUDA 12 cuBLAS runtime that
# faster-whisper (CTranslate2) loads for GPU transcription. The GPU driver itself is
# provided by the NVIDIA container runtime. Docling uses the CPU build of PyTorch.
-r requirements.txt
nvidia-cublas-cu12
